# 加密货币 & 外汇投资监控看板 —— 系统说明 + 免费部署完整教程

## 一、这套系统是什么，能做什么

一个纯免费、部署在网页上的个人投资信息助手，打开网址随时看，不需要你告诉它持仓，
只根据公开数据做技术面提示，辅助你自己判断买卖时机。一共 6 个功能模块：

| 标签页 | 功能 |
|---|---|
| 👀 加密货币关注 | 输入你关心的币（支持直接打代号如 xrp、sui），显示现价、涨跌幅，并基于 RSI、均线给出"关注买入区/偏空/中性"提示 + 具体理由，同时显示该币前10大地址的持仓集中度（巨鲸筹码信号） |
| 💱 外汇关注 | 同样的技术面打分逻辑用在外汇货币对上（如 USD/JPY、EUR/USD），数据源是欧洲央行免费汇率 |
| 🚀 新币动量扫描 | 挖掘刚启动、涨幅异常的新币，按"涨幅+换手强度+买卖力量"综合评分排序，附带项目背景简介、社交链接、合约安全检测 |
| 🐋 巨鲸监控 | 输入你关注的巨鲸钱包地址，查看最近的代币转入/转出活动流水（客观展示，不自动判断买卖方向，避免误导） |
| 📈 信号回测 | 把现在用的打分规则套用到过去一年的真实历史价格上，统计"这个信号出现后未来N天真实涨跌概率"，验证规则靠不靠谱 |
| 📰 每日报告 | 一键把当前几个模块已加载的数据拼成一份文字摘要，快速过一遍全貌 |

顶部还有一个市场概览：BTC市占率 + 恐慌贪婪指数，帮你判断当前大盘情绪。

全部数据源都是免费公开API（CoinGecko、DexScreener、GoPlus Security、欧洲央行、
alternative.me），除了"巨鲸监控"需要你自己申请一个免费的 Etherscan API Key 之外，
其余模块打开就能用，不需要任何配置。

**再次强调**：所有提示都是基于公开技术指标的规则打分，不是预测，更不构成投资建议，
最终买卖决策需要你自己判断。

---

## 二、部署教程（全程免费，大概20分钟）

### 第一步：把代码传到 GitHub

1. 没有 GitHub 账号先注册：https://github.com/signup（免费）
2. 新建一个仓库，比如叫 `crypto-monitor`，建议选 **Private**（私有）
3. 把这个代码包里的文件全部上传：`core.py`、`streamlit_app.py`、`app.py`、
   `requirements.txt`、`.gitignore`
   - 网页上传最简单：进入仓库页面点 `Add file → Upload files`，把文件拖进去，点 `Commit changes`
   - 如果之前已经建过仓库，这次只需要把这几个文件重新上传一遍覆盖旧版本即可

### 第二步：部署成网页（二选一）

**方案A：Streamlit Community Cloud**（如果你之前已经用 GitHub 登录成功过，直接用这个）

1. 打开 https://share.streamlit.io，用 GitHub 账号登录
2. 点 `New app` → 选择仓库 `crypto-monitor` → 分支选 `main`
3. Main file path 填 `streamlit_app.py`
4. 点 `Deploy`，等 1-2 分钟

**方案B：Hugging Face Spaces**（如果方案A登录一直报错，用这个代替，不走GitHub登录）

1. 打开 https://huggingface.co/join 免费注册（邮箱即可）
2. 打开 https://huggingface.co/new-space 创建 Space：
   SDK 选 Streamlit（没有就选 Docker → 下一步选 Streamlit 模板），Hardware 选免费 CPU
3. 上传 `app.py`、`core.py`、`requirements.txt` 三个文件（HF 默认识别的入口文件名是 `app.py`）
4. 等 1-2 分钟自动构建

### 第三步：以后随时打开

部署成功后你会拿到一个固定网址，形如：
- Streamlit Cloud: `https://你的名字-crypto-monitor-xxxxx.streamlit.app`
- Hugging Face: `https://你的用户名-crypto-monitor.hf.space`

**建议做的事**：
- 把这个网址加到手机浏览器主屏幕（iOS：分享→添加到主屏幕；Android：菜单→添加到主屏幕），
  以后像打开App一样一键打开，不用记网址
- 长时间没访问会"休眠"，重新打开需要等十几秒唤醒，这是免费额度的正常现象，等一下就好

---

## 三、以后代码更新了怎么办

以后我帮你继续优化功能，改动的文件会重新发给你，你只需要：
1. 去 GitHub 仓库把对应文件重新上传覆盖（文件名保持一致）
2. Streamlit Cloud / Hugging Face Space 会自动检测到更新，自动重新构建，1-2分钟后新版本生效
3. 打开网页刷新一下就行，不需要重新走一遍部署流程

## 四、遇到问题怎么排查

- 网页打不开/报错 → Streamlit Cloud 在应用页面点 `Manage app` 看日志；
  Hugging Face 在 Space 页面点 `Logs` 标签看日志
- 某个免费 API 突然大量报错 → 大概率是官方接口调整了，把报错内容发给我，我帮你改代码

## 四次更新说明（设置保存 + 美联储利率倒计时）

- **设置自动保存**：侧边栏最下方多了一个"💾 保存当前设置"按钮，点一下就会把
  你填的关注列表、Etherscan Key、巨鲸地址等全部保存到部署环境自己的文件里。
  下次打开网页会自动读取、自动填充，不用重新输入。
  **需要注意**：这个保存是存在服务器本地文件里的，只要你不重新推送代码触发平台
  重新构建，就会一直保留；如果你以后更新了代码（比如我又帮你加了新功能，
  需要重新上传文件到GitHub），平台重新构建后这个文件会被清空，需要重新点一次保存
- **顶部新增"联邦基金利率"和"距下次议息会议"两个指标**：
  会议倒计时是根据美联储公布的2026年FOMC八次会议固定日程算的，不需要任何配置；
  利率默认显示一份写死的静态快照（会标注日期），如果想要实时利率，
  可以去 https://fred.stlouisfed.org/docs/api/api_key.html 免费申请一个 FRED API Key，
  填到侧边栏"🏦 美联储利率"那一栏，同样记得点"保存当前设置"

以上依然全部免费，FRED Key是可选项，不填也能正常看会议倒计时。

## 五次更新说明（利率驱动的风险市场偏多偏空信号）

- 顶部新增"利率驱动的风险市场参考信号"：根据过去约180天利率的变化趋势
  （降息/加息/持平），给一个宏观流动性层面的"风险资产偏多/偏空"参考框架
- 这个信号**需要你填了 FRED API Key 才能用真实历史数据计算**（跟利率快照用的是同一个Key，
  不用额外申请）；没填Key的时候会显示一份通用的判断框架文字，帮你理解"降息/加息通常
  怎么影响风险资产"，但不会给你一个基于实时数据的具体方向判断
- 这个信号已经同步加进"每日报告"里了

**重要提醒**：这是宏观流动性层面的粗略经验规律（历史上降息周期通常利好风险资产、
加息周期通常承压），不是精确预测。实际行情还受经济数据、市场预期、地缘政治等
很多其他因素影响，尤其"降息"本身还分"预防式降息"（偏正面）和"衰退式降息"（可能先跌后涨）
两种性质完全不同的情况，这套系统目前没法自动区分这两种，需要你自己结合当时的经济
背景再判断，我在文案里也提醒了这一点。

## 六次更新说明（巨鲸每日报告 + 币种简写识别 + 修复获取失败问题）

### 1. 巨鲸监控改成"今日报告"
不再是一条条转入转出流水，而是：
- **按代币聚合今天的净流入/净流出**（比如"净流入15 ETH，净流出5万USDC"），一眼看出今天整体在囤还是在出
- **主要交易对手方分析**：查一下今天交易最频繁的几个对手方，是合约还是普通钱包、
  合约叫什么名字（这是从Etherscan查到的客观事实，不是猜的）。如果对手方是DEX路由类合约，
  会提示"可能是在做链上swap"；如果是普通钱包，会提示"更像是资金归集/分仓/转账"
- 详细的原始流水收进了"查看今天的完整流水明细"折叠框，每笔都带了区块浏览器链接方便你核实
- **老实说清楚一点**：解读部分是基于客观数据的合理推测方向，不是确定结论——链上数据本身
  不包含"意图"，没有任何免费数据源能100%准确告诉你"这笔操作背后是想干嘛"，需要你自己
  点交易链接核实细节再下判断

### 2. 加密货币现在真的只填简写就行
之前 xrp、sui 偶尔查不到，排查下来主要是两个原因，这次一起解决了：
- 内置了一张常见币种代号对照表（btc/eth/xrp/sol/sui/bnb/doge等60多个主流币），
  命中的话直接用，不用再调用搜索接口，基本不会再出现"找不到"的情况
- 对不在表里的生僻币种，加了自动重试和更准确的报错提示（能分清"真的没这个币"
  和"接口被限流了"这两种情况）

### 3. 强烈建议加一个免费的 CoinGecko Demo API Key
之前经常"获取数据失败"，根本原因是CoinGecko完全免费不注册的公开接口只有
**5~15次/分钟**的限速，非常容易触发。免费注册一个Demo Key能稳定在30次/分钟：
1. 打开 https://www.coingecko.com/en/api/pricing → 点 `Create Free Account`
2. 注册登录后进 Developer's Dashboard → API Keys → `+ Add New Key`
3. 把生成的Key填到侧边栏最上面的"🦎 CoinGecko Demo Key"栏，点保存

这个Key全程免费，不需要绑卡，加了之后"关注币种""信号回测"这些模块会明显更稳定。

## 七次更新说明（技术面多因子升级 + 衍生品杠杆情绪层）

参考了你贴的那份"多因子评分系统"文档，把里面**真正能免费做到位**的两块做了进来：

### 1. 技术面从"RSI+简单均线"升级成多因子打分
- **EMA20/50/200 多周期结构**：不再是简单的"现价>MA7>MA25"，现在会判断"现价<EMA200
  但现价>EMA20>EMA50"这种"熊市末期恢复"的结构，以及标准的多头/空头排列
- **MACD柱状图转折**：由负转正/由正转负，以及"柱子在缩小=动能减弱"这种细节
- **RSI背离检测**：价格创新低但RSI没跟着创新低（底背离，卖压减弱）、价格创新高但RSI
  没跟着创新高（顶背离，上涨动能减弱），这比单纯"RSI<30就是超卖"更接近专业交易员的判断方式
- 因为因子变多了，评分的阈值也相应调整了（现在要更多因子共振才会给出"关注买入区"这种
  强信号），"信号回测"那个标签页可以直接验证这套新规则的历史胜率

### 2. 新增"衍生品层"：资金费率 + 未平仓合约
数据源是 **Binance合约公开API，完全免费不需要key**。这是加密货币独有的、反映短期杠杆
情绪是否过热的硬指标：
- 资金费率过高 = 多头拥挤，警惕挤仓回调；过低（负值）= 空头拥挤，可能挤空反弹
- 未平仓合约变化 = 杠杆仓位是在加仓还是在减仓
- 两者结合价格走势，能识别"价格涨+持仓涨+费率涨"这种历史上常见的杠杆过热顶部信号，
  以及"价格跌+持仓降+费率回落"这种杠杆出清后的潜在机会窗口
- 只覆盖 Binance 有合约上架的币种，小市值新币可能没有，会显示"未覆盖"

侧边栏新增了对应的开关，可以选择要不要查这两项（多两次请求，关掉能省点API额度）。

**没做的部分**（上次已经说过原因，这里不重复）：交易所净流入流出、MVRV/SOPR链上估值、
社交媒体AI情绪——这几个免费数据源做不到有意义的精度，保持诚实，没有强行凑数。

## 八次更新说明（挖掘机会拆成三层：新币早期 / 二级市场强势 / 一级市场融资）

原来的"新币动量扫描"标签页改名叫"🚀 挖掘机会"，里面拆成三个子标签：

### 🌱 新币早期扫描（原来的功能，没变）
DexScreener挖的微盘新币，涨幅异常、刚起步的那种。

### 🔥 二级市场强势（新增）
数据源：**CoinGecko trending**，免费，是CoinGecko根据全网实时搜索热度算出来的
"现在大家都在搜什么币"Top7。跟新币扫描互补——这里覆盖的是已经有一定市值、
正在被广泛关注、**叙事（赛道分类）和项目背景都比较清晰**的币种，符合你说的
"有叙事、有背景、优质项目"。

### 🏦 一级市场融资（新增）
数据源：DefiLlama公开的融资数据。列出近期融资事件，命中知名一二线VC
（a16z、Paradigm、Binance Labs等）参投的会标⭐排在前面，作为"值得提前认识"的看好理由。

**这里要对你格外坦白一点**：这个接口不在DefiLlama官方文档列出的正式API目录里，
是他们融资页面本身在用的接口，我没办法在开发环境里直接联调验证（网络权限限制），
字段结构也没有官方保证。**如果你点开这个子标签发现一直报错或者拉不到数据，
大概率是接口的字段名或者路径变了**，把报错信息发给我，我根据实际返回的数据结构改代码；
它同时也会提示你去 https://defillama.com/raises 手动查看，不会让你两手空空。

**关于"打破信息差、提前布局"这件事，我要再泼一点冷水**：一级市场的融资信息本质上
只是"哪些机构在哪个阶段投了钱"，跟"这个项目代币未来会不会大涨"是两件不完全相关的事——
很多融资很猛的项目代币表现平平，也有没什么大牌机构背书的项目代币表现很好。这个模块
的价值是帮你**提前"认识"这些项目**，等它们未来真的上二级市场、进入"新币早期扫描"或者
"二级市场强势"这两个模块的视野时，你已经比只看K线的人多一层背景认知，仅此而已，
不代表看到融资消息就应该冲进去。

## 九次更新说明（多周期共振 + EMA20/60/120 + SMA20动能）

### 1. EMA参数从20/50/200改成20/60/120
统一用在所有周期（15分钟/1小时/日线/周线/外汇的日/周/月）上，数字之间是整数倍关系，
逻辑更整齐。加了一个 **SMA20**，跟EMA20做对比，判断"近期动能是在加速还是减速"
（EMA20明显高于SMA20 = 最近涨得比前段时间猛，加速信号），作为一个小的加减分项，
不单独占一个主力打分维度，避免和其他指标抢权重。

### 2. 新增"多周期共振分析"
在"👀 加密货币关注"和"💱 外汇关注"两个标签页最下面，各加了一个独立的深度分析入口：

**加密货币**：周线(权重4,大趋势) / 日线(权重2,中期) / 小时线(权重1) / 15分钟线(权重1,进场时机)，
数据源Binance现货K线，免费不需要key，能精确指定任意周期。

**外汇**：月线(权重4) / 周线(权重2) / 日线(权重1)。外汇没有免费的分钟级数据，
周线月线是从日线数据自己在本地聚合出来的，不需要额外调接口。

**这是单独的按需查询功能，不会对你整个关注列表自动批量跑**（4个周期×N个币种的请求量太大，
容易被限流），需要你输入一个具体的币种/货币对，点按钮才会查。

### 3. 重要结论用了更醒目的颜色区块
按你的要求，"共振信号越强，颜色越突出"：
- **全部周期一致向上/向下**：绿色/红色的大色块提示框（st.success/st.error），一眼就能看到
- **多数周期偏多/偏空但没完全共振**：黄色提示框，弱化处理
- **各周期打架、方向不明朗**：蓝色提示框，提醒你观望

### 4. 15分钟精准进场点
只在大周期（周线/日线）已经确定的方向上找15分钟的顺势时机，不会用15分钟单独做逆势判断。
触发条件：价格回踩EMA20企稳、RSI从超卖区回升、MACD由负转正（大周期偏空时逻辑相反，
用词也换成"减仓/止盈参考"而不是"做空进场"，因为现货用户更多考虑要不要卖）。

**用法提醒（之前聊过的）**：15分钟信号"保质期"很短，界面会显示"这个信号截至几分钟前"，
超过15分钟建议重新点一次分析，不要拿一个可能已经过期的信号做决策。

## 十次更新说明（多周期共振自动化 + 显示简写 + 编号加粗理由）

按你的反馈修正了三点：

### 1. 多周期共振改成自动展示，不用你一个个手动输入
"👀 加密货币关注"和"💱 外汇关注"里，每一个你填的币种/货币对现在都会**自动**附带
多周期共振分析（不再需要单独跳到一个工具去手动输入查询）。侧边栏新增一个开关
"同时查多周期共振"，默认开启；如果你关注的币种/货币对比较多，会明显变慢（每个都要
多查4个周期），嫌慢的话可以在侧边栏关掉。

### 2. 显示你填写的简写，不再是CoinGecko全称
你填 `eth`，列表标题就显示 `ETH`，不会再显示成 `ETHEREUM`。内部数据请求还是走
CoinGecko的标准id（这个不能省，是接口要求），只是**界面显示**改成了你输入的原始简写。

### 3. 技术面理由改成编号列表，重点内容加粗变色
不再是一行用顿号连起来的长句子，现在是：
```
1. RSI(14)=60，中性
2. EMA尚未形成明显多头或空头排列
3. EMA20高于SMA20 0.5%，近期上涨动能在加速   ← 加粗+绿色
4. MACD柱状图仍为正但在缩小，上涨动能减弱
5. 价格创近期新高但RSI未跟随创新高，顶背离信号  ← 加粗+红色
```
只有**对最终打分有实际影响**的理由（比如背离信号、EMA多空排列确认、MACD转折）才会
加粗变色突出显示；单纯描述现状但不影响打分的（比如"RSI中性""EMA还没排列出方向"）
保持普通文字，这样你一眼就能抓到真正重要的信息，不会被信息量淹没。杠杆情绪那块也
用了同样的编号格式。

## 十一次更新说明（修复+优化五项）

### 1. 巨鲸集中度检测不支持时不再显示提示
之前查不到集中度数据会显示"🐋 该链暂不支持集中度检测"占地方，现在直接不显示这一行。

### 2. 多周期共振性能优化
之前15分钟这一档会被请求两次（一次算共振、一次单独找进场点），现在改成只请求一次、
复用同一份数据，减少了25%的请求量，理论上会更快更稳定。**如果你之前反馈"没有显示多周期共振"，
大概率是这几种情况**：①还没重新部署最新代码 ②侧边栏"同时查多周期共振"开关被关了
③这个币在Binance没有对应的USDT交易对（换个主流币试试）④网络请求较多导致偶尔超时，
刷新重试一次。现在如果某个币的共振数据没查到，会明确提示原因，不会什么都不显示。

### 3. 技术面理由标注了周期
"技术面理由"前面现在会写清楚是"（日线）"的分析结果，跟下面"多周期共振"里的
15分钟/1小时/日线/周线区分开，避免你搞混这两块说的是不是同一个东西。

### 4. 信号回测改成下拉框选择
不用再手动打字输入币种/货币对了，直接从你在侧边栏填的关注列表里选，
还多了"全部关注币种/全部关注货币对"选项，一次性把你关注的全部回测一遍。

### 5. 新增"近期重要宏观事件"提醒
在市场概览区域，新增了美国CPI通胀数据、FOMC利率决议、非农就业数据这三类对美联储利率
决策影响最大的事件提醒：
- **CPI和FOMC日期**来自官方公开日程（美国劳工统计局BLS、美联储官网），相对准确
- **非农就业数据**是按"每月第一个周五"规则估算的，遇到节假日BLS可能顺延1天，仅供参考
- 如果**3天内**有CPI或FOMC这种高影响力事件，会用醒目的红色提示框提醒你
  "建议空仓观望，不要轻易下单"；更远一点的事件用蓝色提示框弱化展示
- 这个提醒也同步加进了"每日报告"里

**说清楚这个提醒的逻辑**：不是说这几天一定会暴涨暴跌，而是历史上这类数据/决议公布前后，
市场波动通常会明显放大，方向也很难预判，仓位控制和风险管理在这几天比平时更重要，
这是给你的一个"多加小心"的提醒，不是"一定会跌所以要清仓"的预测。
